Vector database → stores vectors
Vector index → organizes vectors for faster search
Similarity search → finds similar vectors
Cosine similarity → measures how similar their directions are
Top K → number of results we want back

.search(query_vector, 3) meaning “Search using this query vector and give me the 3 most similar results.”
